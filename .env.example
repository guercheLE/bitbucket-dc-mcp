# Bitbucket Data Center Configuration (required for Stories 2.x+)
# BITBUCKET_URL=https://bitbucket.example.com

# Authentication (configure in later stories)
# BITBUCKET_PAT=your_personal_access_token_here

# Embedding Model Configuration (optional, defaults provided)
# EMBEDDING_MODEL=Xenova/all-mpnet-base-v2
# BATCH_SIZE=32

# Logging Configuration (optional, defaults provided)
# Log level: DEBUG, INFO, WARN, ERROR (default: INFO)
LOG_LEVEL=INFO

# Log output: stdout, file, both (default: stdout)
LOG_OUTPUT=stdout

# Log file path (when LOG_OUTPUT=file or both)
LOG_FILE_PATH=./logs/bitbucket-dc-mcp.log

# Log rotation: daily, hourly (default: daily)
LOG_ROTATION=daily

# Max log file size in MB before rotation (default: 100)
LOG_MAX_SIZE=100

# Max log files to keep (default: 7)
LOG_MAX_FILES=7

# Pretty print logs for development (default: false)
# LOG_PRETTY=true
